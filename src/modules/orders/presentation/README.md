# Orders Presentation

Orders admin and storefront presentation code should migrate here incrementally.

Legacy components under `src/components/admin/orders/**` remain valid during the transition and should be moved behind module exports in focused slices.
